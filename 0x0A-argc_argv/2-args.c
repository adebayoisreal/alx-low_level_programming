#include <stdio.h>

/**
 * main - code
 * @argc: code
 * @argv: code
 *
 * Description: code
 * Return: code
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
		printf("%s\n", argv[i]);

	return (0);
}
