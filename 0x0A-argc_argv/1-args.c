#include <stdio.h>

/**
 * main - code
 * @argc: code
 * @argv: code
 *
 * Description: code
 * Return: code
 */
int __attribute__((unused)) main(int argc, char *argv[])
{
	(void) *argv;
	printf("%d\n", (argc - 1));

	return (0);
}
