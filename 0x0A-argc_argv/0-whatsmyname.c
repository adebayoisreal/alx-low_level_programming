#include <stdio.h>

/**
 * main - code
 * @argc: code
 * @argv: code
 *
 * Description: code
 * Return: code
 */
int __attribute__((unused)) main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
